# OpenAPI Specification

barrel_docdb is embedded-only and no longer serves HTTP, so it publishes no
OpenAPI document. The HTTP surface (and any OpenAPI/Redoc output) belongs to
`barrel_server`. See [http.md](http.md) and the `barrel_server` REST guide
(`docs/guides/rest-server.md` in the umbrella).
