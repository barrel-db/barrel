# barrel_docdb Design

The design document lives at [`docs/design.md`](docs/design.md). It is the single canonical source: the version model (HLC versions and version vectors), the v4 entity codec, retained history and the per-doc version chain, the replication protocol, timelines, and the storage layout.

Read it there so this repo keeps one design document, not two that drift apart.
