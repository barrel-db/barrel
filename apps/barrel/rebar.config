{erl_opts, [debug_info, warnings_as_errors]}.

%% barrel_docdb and barrel_vectordb are local umbrella apps, discovered
%% automatically. When this package is published to Hex they are declared as
%% Hex dependencies via the `hex' profile below.
{deps, []}.

{project_plugins, [rebar3_hex, rebar3_ex_doc]}.

{eunit_opts, [verbose]}.
{cover_enabled, true}.
{ct_opts, [{verbose, true}]}.

%% Facade API: every export is consumed by external callers, so do not enable
%% exports_not_used.
{xref_checks, [
    undefined_function_calls,
    undefined_functions,
    locals_not_used
]}.

{profiles, [
    {hex, [
        {deps, [
            {barrel_docdb, "~> 0.8"},
            {barrel_vectordb, "~> 2.0"}
        ]}
    ]}
]}.

{hex, [{doc, ex_doc}]}.

{ex_doc, [
    {source_url, <<"https://github.com/barrel-db/barrel">>},
    {extras, [<<"README.md">>]},
    {main, <<"readme">>},
    {api_reference, true}
]}.
